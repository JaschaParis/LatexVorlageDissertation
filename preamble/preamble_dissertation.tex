\documentclass[
	ngerman,					% ngerman / english	, 
	headsepline,				% Horizontal line after header
	open			= right,	% right opened
	%cleardoublepage = empty 	% @Jascha: Nachschlagen, kommt von MA
	draft 			= false,	% Document status [true = draft | false = final]
	twoside			= true,		% Tocument Side Type [true = two-sided/duplex | false = single-sided]
	paper 			= a4,
	parskip			= half,
	fontsize		= 12pt,
	pagesize,			
	DIV				= 14,		% Calculate DIV (calc) or set numeric value
	BCOR 			= 8mm,	
	toc				= graduated,
	toc 			= chapterentrywithdots,
	listof			= totoc,	
	listof			= flat,	
	bibliography	= totoc,	% Bibliography in table of content
	headinclude		=true,		% Header is part of text body
	footinclude		=false,		% Footer is part of text body
	headings=small,
	fleqn						% {equation} left justify
	]{scrbook}          		% Koma-Script
	
% -------- %	
% Packages %
% -------- %

% PDF Versionen for high compression ratio
\pdfminorversion  = 7
\pdfcompresslevel = 9 

% Load custom dissertation package and set template data
\usepackage{packages/mech-eng-dissertation}
\input{template/setData}
\input{template/setAdditions}

\AfterBibliographyPreamble{\raggedright}

% Load custom TikZ package and set styles for embedded TikZ graphics
\usepackage{packages/mech-eng-dissertation-tikz}
\input{template/setTikZStyles}
